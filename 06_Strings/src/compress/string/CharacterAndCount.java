package compress.string;

import java.io.Console;

/*
 * finding difficulty in replacing the characters.
 * inplace conversion might be difficult
 * 
 * use another string to store the new string with count
 */
public class CharacterAndCount {

	public static void main(String[] args) {

		//String str = "aaabbcddddde";
		
		
			/*String str = "aaaabbccccc";
			int count = 0;
			for (int i = 0; i < str.length(); i++)
			{
			
			new Console().readLine();
				
			//Console.WriteLine(str[i]);
			
			count = 1;
			while (i + 1 < str.length() && str[i] == str[i+1])
			{
			count++; i++;
			continue;
			}
			Console.WriteLine(count);
			count = 0;
			}
			Console.ReadLine();*/
	}

	public static void compressString(String str) {

		/*int l = str.length();
		int count = 0;

		for (int i = 0; i < l-1; i++) {
			
			if(str.charAt(i) == str.charAt(i)+1){
				count++;
				continue;
			}else{
				//str = str.re
			}
		}*/

	}

}
